{
  "DatabaseSettings": {
    "ConnectionString": "Host=localhost;Port=5432;User=postgres;Password=postgres;Database=test_database"
  },

  "JobSettings": {
    "RepeatInterval": "0 * * ? * *" //Every minute
  }
}
